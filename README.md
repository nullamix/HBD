# hbd

An Astro birthday page for GitHub Pages.

Use a path-only name:

```text
/hbd/your_name
```

The app reads the name from the path. Query strings and on-page name input are intentionally not used.

GitHub Pages serves arbitrary path names like `/hbd/sunny` through the generated `404.html` fallback, so the page can render even though the route is dynamic.
