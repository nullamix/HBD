# HBD

A static birthday page for GitHub Pages.

Use either URL shape:

```text
/hbd/sunny
/hbd?n=sunny
```

`index.html` handles query-string names. `404.html` mirrors the same app so GitHub Pages can render path-style names like `/hbd/sunny`.
